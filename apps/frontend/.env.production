# Production builds (pnpm build / preview / deploy). Committed on purpose —
# public values only. The Cloud Run URL is stable for the life of the
# service; if it ever changes (rename/recreate) this is the one line to edit.
VITE_API_URL=https://backend-6q35zrinhq-uw.a.run.app
